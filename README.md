# Quiz-Leaderboard-
This project is a Java Swing quiz leaderboard application. Structured using OOP model, service, gui, it accepts new scores via a form, instantly ranks the entries (highest score first), and displays the results in a real-time JTable.
